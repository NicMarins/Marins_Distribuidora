# Ambiente
NODE_ENV=development
PORT=2000

# Banco de dados (PostgreSQL)
DATABASE_URL=

# Banco de dados usado por `npm test` (Etapa 9) — use um banco DESCARTÁVEL,
# nunca aponte para produção ou desenvolvimento: o teste apaga o schema
# inteiro a cada execução.
TEST_DATABASE_URL=

# Autenticação
JWT_SECRET=
JWT_EXPIRES_IN=
COOKIE_SECRET=

# Upload
MAX_UPLOAD_SIZE_MB=
