// public/js/admin.js
// Interações do painel administrativo. Nesta etapa (Frontend) o painel é
// apenas visual; ações reais de criar/editar/excluir serão conectadas à
// API nas Etapas 4 e 6.

document.addEventListener('DOMContentLoaded', () => {
  console.log('Painel administrativo carregado.');
});
